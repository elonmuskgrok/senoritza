server:
  port: 8080

spring:
  application:
    name: taxtracker-backend
  datasource:
    url: jdbc:mysql://localhost:3306/taxtracker_db?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: siva
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect
  sql:
    init:
      mode: never
  mail:
    host: smtp.gmail.com
    port: 587
    username: ${MAIL_USERNAME:elonmuskgrok9@gmail.com}
    password: ${MAIL_PASSWORD:tfae ohlr pobi vloz}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true


app:
  jwt:
    secret: "eHlwcnpvcTEyMzQ1Njc4OWFzZGZnaGprbG1ub2JxcnN0dXZ3eHl6MTIzNDU2Nzg5MA==" # Base64 encoded secret for HS256 (at least 256 bits)
    expirationMs: 3600000 # 1 hour
